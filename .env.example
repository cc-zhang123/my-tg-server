# docker-compose-env 环境变量模板
# 使用方式: 复制为 .env 后按需修改密码
# cp .env.example .env

# ---------- MySQL ----------
# ⚠️ MYSQL_PASSWORD 不要带 @ / : / ? / & 等特殊字符,会和 Go DSN 解析冲突
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=teamgram
MYSQL_USER=teamgram
MYSQL_PASSWORD=Mysql1008613.

# ---------- MinIO ----------
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=Minio1008613.

# ---------- Grafana(已移除,可忽略) ----------
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=GRAFANA1008613.
GRAFANA_ROOT_URL=http://154.201.84.21:3000
